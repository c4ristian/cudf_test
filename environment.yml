# YAML for setting up a conda environment for cuDF testing
name: cudf_test
channels:
  - rapidsai
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - cuda-version=12.0
  - cudf=24.06
  - numpy
  - pandas
  - pytest
  - jupyter
  - pip

